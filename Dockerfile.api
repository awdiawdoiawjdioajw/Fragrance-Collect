FROM node:18-alpine

WORKDIR /app

# Minimal package.json to install only prod deps if any
COPY api/package.json ./
RUN npm install --only=production || true

COPY api/ ./

ENV NODE_ENV=production

EXPOSE 3000

CMD ["node", "index.js"]


